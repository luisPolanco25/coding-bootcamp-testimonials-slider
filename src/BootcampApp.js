import React from 'react';
import { DisplayedCard } from './components/DisplayedCard';
import './styles.css';

export const BootcampApp = () => {
    return (
        <div>
            <DisplayedCard />
        </div>
    )
}
